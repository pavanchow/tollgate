# The smallest useful policy: one key, one authed and rate-limited route.
key demo-key scopes read

route {
  method GET
  host localhost
  path /api/:resource
  auth scope read
  rate 2/s burst 3
  upstream 127.0.0.1:9000
  set_header X-Tollgate demo
}
